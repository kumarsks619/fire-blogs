service cloud.firestore {
	match /database/{database}/documents {
  	match /users/{userID} {
    	allow read;
      allow write: if request.auth.uid != null;
    }
    match /blogs/{postID} {
    	allow read;
      allow write: if request.auth.uid != null;
    }
  }
}